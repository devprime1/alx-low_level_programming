#include <stdio.h>
#include "main.h"

/**
 * main - prints name of a program
 * @argc: number of command line arguments
 * @argv: arguments
 * Return: 0.
 */

int main(int argc, *argv[])
{
(void)argc;
printf("%s\n", argv[0]);

return (0);
}
